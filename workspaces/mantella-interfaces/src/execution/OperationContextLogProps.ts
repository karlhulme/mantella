/**
 * Represents the properties of the log function that is supplied
 * to an operation via the context object.
 */
export interface OperationContextLogProps {
  /**
   * The message to log.
   */
  message: string
}
