import { OperationRecord } from './OperationRecord'

/**
 * Defines the properties passed to the save operation function.
 */
 export interface SaveOperationProps {
  /**
   * The operation record to save.
   */
  record: OperationRecord
}
